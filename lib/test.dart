import 'package:flutter/material.dart';

class testWidght extends StatelessWidget {
  const testWidght({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
